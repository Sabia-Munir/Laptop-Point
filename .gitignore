node_modules/
.next/
.env
.env.local
.DS_Store
dist/
build/
*.log

# Browser "Save Page As" exports (e.g. a saved Supabase dashboard page) —
# these pull in hundreds of unrelated JS/CSS files and don't belong in the repo.
*_files/
*.download
